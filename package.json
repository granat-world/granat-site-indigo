{
  "name": "granat-site-indigo",
  "version": "1.0.0",
  "description": "Granat partner-facing site — periwinkle indigo (TMA-aligned) variant. Multi-section narrative + voice-to-choir demo. Forks granat.world's Express + voice-API proxy pattern.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "multer": "^2.0.1"
  }
}
